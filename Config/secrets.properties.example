# Copy this file to secrets.properties and fill in the value.
# secrets.properties is listed in .gitignore and must never be committed.
#
# For Gmail this is an app password, not the account password:
#   Google Account > Security > 2-Step Verification > App passwords
# Google displays it in groups of four; the spaces are stripped automatically.
mail.password=
